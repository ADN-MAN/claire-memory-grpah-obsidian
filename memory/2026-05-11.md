# 2026-05-11

## Session Log
- Imported structure from `obsidian-openclaw-memory` as a starter memory graph.
- Set up folders for `memory/`, `second-brain/`, `directives/`, `execution/`, `trigger/`, and `skills/`.
- Created starter notes and wiki-links so Obsidian Graph View has immediate edges.

## Decisions Made
- Keep `MEMORY.md` curated and short.
- Use `memory/YYYY-MM-DD.md` for raw logs.
- Use `second-brain/` for structured knowledge and project context.

## Things to Remember
- Bootstrap identity/user details still need to be filled in collaboratively.
- Obsidian should point at `/root/.openclaw/workspace` as the vault folder.
- `qmd` is not currently installed on this host, so semantic indexing is not active yet.
